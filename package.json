{
  "name": "projects",
  "version": "1.0.0",
  "description": "packageforprojects",
  "main": "algo.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "browser-sync start --server --files \"index.html, css/*.css , js/*.js\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "browser-sync": "^2.18.8"
  }
}
